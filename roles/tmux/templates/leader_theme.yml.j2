# From share.yml (ansible-playbook main.yml --tags tmux)
purple: "{{ theme.purple }}"
cyan: "{{ theme.cyan }}"
yellow: "{{ theme.yellow }}"
pink: "{{ theme.pink }}"
fg: "{{ theme.fg }}"
comment: "{{ theme.comment }}"
comment_bright: "{{ theme.comment_bright }}"
bg: "reset"
pill_fg: "{{ theme.bg }}"
selection: "{{ theme.selection }}"
