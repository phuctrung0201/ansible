fish_brew_packages:
  - fish
  - carapace
  - zoxide
  - bat
  - eza
  - fzf
  - lastpass-cli

fish_config_dir: "{{ lookup('env', 'HOME') }}/.config/fish"

# Apple Silicon Homebrew prefix (fixed across all machines).
homebrew_prefix: /opt/homebrew
