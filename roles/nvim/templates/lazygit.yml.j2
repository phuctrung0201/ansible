# AUTO-GENERATED from share.yml. Do not edit by hand.
gui:
  nerdFontsVersion: "3"
  theme:
    activeBorderColor:
      - "{{ theme.peach }}"
      - bold
    inactiveBorderColor:
      - "{{ theme.subtext0 }}"
    searchingActiveBorderColor:
      - "{{ theme.yellow }}"
    optionsTextColor:
      - "{{ theme.blue }}"
    selectedLineBgColor:
      - "{{ theme.surface0 }}"
    inactiveViewSelectedLineBgColor:
      - "{{ theme.overlay0 }}"
    cherryPickedCommitFgColor:
      - "{{ theme.blue }}"
    cherryPickedCommitBgColor:
      - "{{ theme.surface1 }}"
    markedBaseCommitFgColor:
      - "{{ theme.blue }}"
    markedBaseCommitBgColor:
      - "{{ theme.yellow }}"
    unstagedChangesColor:
      - "{{ theme.red }}"
    defaultFgColor:
      - "{{ theme.text }}"
  authorColors:
    "*": "{{ theme.lavender }}"
