{# Window borders — colors from share.yml (same pairing as Ghostty active/inactive borders). #}
{% set _border_active = theme.purple | replace("#", "") | upper %}
{% set _border_inactive = theme.comment_bright | replace("#", "") | upper %}
after-login-command = []

after-startup-command = [
'exec-and-forget borders active_color=0xEE{{ _border_active }} inactive_color=0x40{{ _border_inactive }} width=5.0'
]

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'
# Mouse follows focused monitor (see https://nikitabobko.github.io/AeroSpace/commands#move-mouse)
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']
# Mouse follows focused window or workspace when focus changes
on-focus-changed = ['move-mouse window-lazy-center']
automatically-unhide-macos-hidden-apps = false

# Run when the focused workspace changes. Doc pattern: https://nikitabobko.github.io/AeroSpace/guide#exec-on-workspace-change-callback
# Same shape as the SketchyBar example: /bin/bash -c "<script>" (argv0 is not resolved via PATH; the shell finds im-select).
# Env: AEROSPACE_FOCUSED_WORKSPACE, AEROSPACE_PREV_WORKSPACE. English layout id: run `im-select` with no args to print current.
# No [exec] block: AeroSpace defaults already prepend /opt/homebrew to PATH for exec-* (guide §2.7). Add [exec.env-vars] only for Intel Homebrew (/usr/local), ~/bin, etc.
exec-on-workspace-change = ['/bin/bash', '-c', 'im-select com.apple.keylayout.ABC']

[key-mapping]
preset = 'qwerty'
[gaps]
inner.horizontal = 8
inner.vertical = 8
outer.left = 4
outer.bottom = 4
outer.top = 4
outer.right = 4

# Window management: Option (⌥) + key. On macOS, AeroSpace's `alt-` prefix is the Option key.
# No separate binding mode — chords run directly from main mode.
# Workspaces: `=terminal 1=browser 2=fallback 3=empty F1=slack F2=outlook F3=zoom
[mode.main.binding]

# tab/space in the old action mode → Option+key
alt-tab = 'focus --boundaries-action wrap-around-the-workspace dfs-next'
alt-space = 'focus-back-and-forth'
alt-shift-space = 'workspace --wrap-around next'

alt-backtick = 'workspace `'
alt-shift-backtick = 'move-node-to-workspace --focus-follows-window `'

# Focus (hjkl — left/down/up/right)
alt-h = 'focus left'
alt-j = 'focus down'
alt-k = 'focus up'
alt-l = 'focus right'

# Move windows (Option+Shift+hjkl)
alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'

# Workspaces
alt-1 = 'workspace 1'
alt-2 = 'workspace 2'
alt-3 = 'workspace 3'
alt-f3 = 'workspace F3'
alt-f2 = 'workspace F2'
alt-f1 = 'workspace F1'
alt-shift-1 = 'move-node-to-workspace --focus-follows-window 1'
alt-shift-2 = 'move-node-to-workspace --focus-follows-window 2'
alt-shift-3 = 'move-node-to-workspace --focus-follows-window 3'
alt-shift-f3 = 'move-node-to-workspace --focus-follows-window F3'
alt-shift-f2 = 'move-node-to-workspace --focus-follows-window F2'
alt-shift-f1 = 'move-node-to-workspace --focus-follows-window F1'

# Monitor (brackets)
alt-leftSquareBracket = 'focus-monitor --wrap-around prev'
alt-rightSquareBracket = 'focus-monitor --wrap-around next'
alt-shift-leftSquareBracket = 'move-workspace-to-monitor --wrap-around prev'
alt-shift-rightSquareBracket = 'move-workspace-to-monitor --wrap-around next'

# Layout
alt-t = 'layout accordion tiles'
alt-f = 'layout floating tiling'

# Resize
alt-minus = 'resize smart -50'
alt-equal = 'resize smart +50'

# Service
alt-r = 'reload-config'
alt-x = 'close'
alt-shift-x = 'close-all-windows-but-current'

# List current opening apps
# aerospace list-apps
[[on-window-detected]]
if.app-id = 'com.mitchellh.ghostty'
run = 'move-node-to-workspace --focus-follows-window `'

[[on-window-detected]]
if.app-id = 'app.zen-browser.zen'
run = 'move-node-to-workspace --focus-follows-window 1'

[[on-window-detected]]
if.app-id = 'com.tinyspeck.slackmacgap'
run = 'move-node-to-workspace --focus-follows-window F1'

[[on-window-detected]]
if.app-id = 'com.microsoft.Outlook'
run = 'move-node-to-workspace --focus-follows-window F2'

[[on-window-detected]]
if.app-id = 'us.zoom.xos'
run = 'move-node-to-workspace --focus-follows-window F3'

[[on-window-detected]]
check-further-callbacks = true
run = 'move-node-to-workspace --focus-follows-window 2'
