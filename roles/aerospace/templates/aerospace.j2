after-login-command = []

after-startup-command = [
'exec-and-forget borders active_color=0xFF{{ theme.purple | replace("#", "") | upper }} inactive_color=0xFF{{ theme.selection | replace("#", "") | upper }} width=5.0'
]

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'
on-focused-monitor-changed = []
on-focus-changed = []
automatically-unhide-macos-hidden-apps = false
[key-mapping]
preset = 'qwerty'
[gaps]
inner.horizontal = 8
inner.vertical = 8
outer.left = 4
outer.bottom = 4
outer.top = 4
outer.right = 4

# Main mode is passive — all keys pass through to apps.
# alt-\ enters action mode.
[mode.main.binding]
alt-backslash = 'mode action'

# Action mode — persistent window management.
# Workspaces: `=terminal 1=browser 2=fallback 3=empty F1=slack F2=outlook F3=zoom
[mode.action.binding]

shift-tab = 'workspace --wrap-around prev'
tab = 'focus --boundaries workspace --boundaries-action wrap-around-the-workspace dfs-prev'
backtick = ['workspace --auto-back-and-forth `', 'mode main']
shift-backtick = ['move-node-to-workspace --focus-follows-window `', 'mode main']
backslash = ['focus-back-and-forth', 'mode main']
space = ['workspace-back-and-forth', 'mode main']

# Focus (hjkl)
h = ['focus left', 'mode main']
j = ['focus down', 'mode main']
k = ['focus up', 'mode main']
l = ['focus right', 'mode main']

# Move windows (shift-hjkl)
shift-h = ['move left', 'mode main']
shift-j = ['move down', 'mode main']
shift-k = ['move up', 'mode main']
shift-l = ['move right', 'mode main']

# Workspaces
1 = ['workspace 1', 'mode main']
2 = ['workspace 2', 'mode main']
3 = ['workspace 3', 'mode main']
f3 = ['workspace F3', 'mode main']
f2 = ['workspace F2', 'mode main']
f1 = ['workspace F1', 'mode main']
shift-1 = ['move-node-to-workspace --focus-follows-window 1', 'mode main']
shift-2 = ['move-node-to-workspace --focus-follows-window 2', 'mode main']
shift-3 = ['move-node-to-workspace --focus-follows-window 3', 'mode main']
shift-f3 = ['move-node-to-workspace --focus-follows-window F3', 'mode main']
shift-f2 = ['move-node-to-workspace --focus-follows-window F2', 'mode main']
shift-f1 = ['move-node-to-workspace --focus-follows-window F1', 'mode main']

# Monitor (brackets)
leftSquareBracket = 'focus-monitor --wrap-around prev'
rightSquareBracket = 'focus-monitor --wrap-around next'
shift-leftSquareBracket = 'move-workspace-to-monitor --wrap-around prev'
shift-rightSquareBracket = 'move-workspace-to-monitor --wrap-around next'

# Layout
t = ['layout accordion tiles', 'mode main']
f = ['layout floating tiling', 'mode main']

# Resize
minus = ['resize smart -50', 'mode main']
equal = ['resize smart +50', 'mode main']

# Service
r = ['reload-config', 'mode main']
x = ['close', 'mode main']
shift-x = ['close-all-windows-but-current', 'mode main']

esc = 'mode main'

# List current opening apps
# aerospace list-apps
[[on-window-detected]]
if.app-id = 'net.kovidgoyal.kitty'
run = 'move-node-to-workspace --focus-follows-window `'

[[on-window-detected]]
if.app-id = 'app.zen-browser.zen'
run = 'move-node-to-workspace --focus-follows-window 1'

[[on-window-detected]]
if.app-id = 'com.tinyspeck.slackmacgap'
run = 'move-node-to-workspace --focus-follows-window F1'

[[on-window-detected]]
if.app-id = 'com.microsoft.Outlook'
run = 'move-node-to-workspace --focus-follows-window F2'

[[on-window-detected]]
if.app-id = 'us.zoom.xos'
run = 'move-node-to-workspace --focus-follows-window F3'

[[on-window-detected]]
check-further-callbacks = true
run = 'move-node-to-workspace --focus-follows-window 2'
