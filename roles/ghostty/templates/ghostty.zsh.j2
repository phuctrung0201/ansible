# Attach to or create the main tmux session when launching Ghostty
if [[ -n "$GHOSTTY_RESOURCES_DIR" ]]; then
  if command -v tmux &>/dev/null && [[ -z "$TMUX" ]]; then
    tmux new-session -A -s main
  fi
fi
